#include<stdio.h>
int main()
{
	int array[1000], change = 0, num = 0;
	scanf("%d", &num);
	for (int i = 0; i < num; i++)
	{
		scanf("%d", &array[i]);
	}
	for (int i = 0; i < num ; i++)
	{
		for (int j = 0; j < num - 1; j++)
		{
			if (array[j] > array[j + 1])
			{
				change = array[j];
				array[j] = array[j + 1];
				array[j + 1] = change;
			}
		}
	}
		for (int i = 0; i < num; i++)
		{
			if (i != num - 1)
				printf("%d ", array[i]);
			else
				printf("%d", array[i]);
		}
}
